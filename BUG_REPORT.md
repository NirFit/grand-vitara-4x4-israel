# דו"ח באגים – Suzuki Grand Vitara 4x4 Israel

**תאריך בדיקה:** 10 מרץ 2025  
**סביבות בדיקה:** דסקטופ (1280×800), מובייל (375×812)

---

## באגים שתוקנו

### 1. אנימציית כרטיסי הקישורים (animate-item) לא פעלה
- **תיאור:** כרטיסי "מאגר המידע" ו"קישורים נוספים" עם המחלקה `animate-item` לא קיבלו אנימציה בגלילה.
- **סיבה:** ה-JavaScript צפה רק ב-`.animate-on-scroll` ולא ב-`.animate-item`.
- **תיקון:** הוספת `.animate-item` ל-IntersectionObserver ב-`script.js`.

### 2. כפתור "חזרה למעלה" בצד הלא נכון ב-RTL
- **תיאור:** הכפתור היה בצד שמאל, בעוד שבממשק RTL הוא צריך להיות בצד ימין (התחלת הקריאה).
- **תיקון:** שינוי מ-`left` ל-`right` ב-CSS.

### 3. פס התקדמות הגלילה – כיוון לא מתאים ל-RTL
- **תיאור:** הפס גדל משמאל לימין, בניגוד לכיוון הקריאה בעברית.
- **תיקון:** הוספת `direction: rtl` ל-`.scroll-progress`.

### 4. שגיאת קונסול – web-share
- **תיאור:** הופיעה שגיאה `Unrecognized feature: 'web-share'` ב-iframe של יוטיוב.
- **תיקון:** הסרת `web-share` מ-`allow` בשני ה-iframes.

### 5. נגישות – aria-valuenow לא מתעדכן
- **תיאור:** ל-progressbar של הגלילה היו `aria-valuemin` ו-`aria-valuemax` אך `aria-valuenow` לא עודכן.
- **תיקון:** עדכון `aria-valuenow` ב-JavaScript לפי אחוז הגלילה.

### 6. כפילות ב-CSS
- **תיאור:** הגדרות כפולות ל-`.hero-badge`, `.footer-content`, `.footer-brand`, `.footer-logo`, `.footer-copy`.
- **תיקון:** איחוד ההגדרות והסרת כפילויות.

### 7. התאמה למובייל – כפתור "חזרה למעלה"
- **תיאור:** במובייל הכפתור היה גדול מדי ויכול היה להפריע.
- **תיקון:** הקטנת הכפתור (44px) והתאמת המרווחים ב-media query.

### 8. רשת הסרטונים במובייל
- **תיאור:** `minmax(380px, 1fr)` עלול לגרום לגלילה אופקית במסכים צרים.
- **תיקון:** שימוש ב-`minmax(0, 1fr)` ב-media query למובייל.

---

## המלצות לשיפור (לא באגים קריטיים)

### 1. תפריט המבורגר במובייל
- **תיאור:** בכותרות יש 3 קישורי ניווט שתמיד גלויים; במסכים קטנים מאוד (למשל 320px) זה עלול להיות צפוף.
- **המלצה:** לשקול תפריט המבורגר מתקפל במסכים קטנים מאוד.

### 2. Safe Area
- **תיאור:** במובייל עם notch או כפתורי ניווט, כפתור "חזרה למעלה" עלול להימצא מתחת ל-notch.
- **המלצה:** שימוש ב-`env(safe-area-inset-bottom)` ו-`env(safe-area-inset-right)` למיקום הכפתור.

### 3. טעינת תמונה
- **תיאור:** הלוגו נטען מ-URL חיצוני; אם הטעינה נכשלת, מוצג fallback (אימוג'י).
- **סטטוס:** כבר מטופל ב-`onerror` ב-HTML.

---

## סיכום

| קטגוריה        | כמות |
|----------------|------|
| באגים שתוקנו  | 8    |
| המלצות         | 3    |

האתר נבדק בדסקטופ ובמובייל, והבאגים שתוקנו משפרים את הפונקציונליות, הנגישות וההתאמה ל-RTL.
